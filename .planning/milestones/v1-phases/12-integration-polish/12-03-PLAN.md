---
phase: 12-integration-polish
plan: "03"
type: execute
wave: 3
depends_on:
  - "12-01"
  - "12-02"
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/12-integration-polish/12-03-SUMMARY.md
autonomous: true
requirements:
  - SC12-4
  - SC12-5
must_haves:
  truths:
    - "A fresh source checkout can follow the root README runbook to run npm install and start the TUI."
    - "The source-install dry-run exercises the normal postinstall path and proves the TUI reaches durable startup text."
    - "Every v1 REQ-id in .planning/REQUIREMENTS.md is either complete with traceability evidence or has an explicit follow-up."
    - "12-01 remains the backend prompt-to-main lifecycle proof and 12-02 remains the operator-visible TUI golden-path proof."
  artifacts:
    - path: "README.md"
      provides: "Root source-install runbook for fresh checkouts"
      contains: "npm install"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Final v1 requirement traceability green-out"
      contains: "Complete"
    - path: ".planning/phases/12-integration-polish/12-03-VALIDATION.md"
      provides: "Validation architecture for SC12-4 and SC12-5"
      contains: "Phase 12-03 Validation Architecture"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "Documented source-install and run scripts match package scripts"
      pattern: "npm run tui"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/12-integration-polish/12-01-SUMMARY.md"
      via: "SC12-1/2 lifecycle and verify evidence"
      pattern: "prompt-to-main"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/12-integration-polish/12-02-SUMMARY.md"
      via: "SC12-3 TUI E2E evidence"
      pattern: "golden path tui e2e smoke"
---

<objective>
Execute Phase 12 plan 12-03: create the root README source-install runbook, verify it with a fresh-clone dry-run, and close v1 requirement traceability.

Purpose: satisfy Phase 12 success criteria 4 and 5, then update roadmap/state to reflect Phase 12 completion.
</objective>

<execution_context>
@/home/alpine/.claude/get-shit-done/workflows/execute-plan.md
@/home/alpine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/alpine/vcode0/.planning/PROJECT.md
@/home/alpine/vcode0/.planning/ROADMAP.md
@/home/alpine/vcode0/.planning/STATE.md
@/home/alpine/vcode0/.planning/REQUIREMENTS.md
@/home/alpine/vcode0/.planning/phases/12-integration-polish/12-CONTEXT.md
@/home/alpine/vcode0/.planning/phases/12-integration-polish/12-01-SUMMARY.md
@/home/alpine/vcode0/.planning/phases/12-integration-polish/12-02-SUMMARY.md
@/home/alpine/vcode0/.planning/phases/12-integration-polish/12-03-CONTEXT.md
@/home/alpine/vcode0/.planning/phases/12-integration-polish/12-03-RESEARCH.md
@/home/alpine/vcode0/.planning/phases/12-integration-polish/12-03-PATTERNS.md
@/home/alpine/vcode0/.planning/phases/12-integration-polish/12-03-VALIDATION.md
@/home/alpine/vcode0/package.json
@/home/alpine/vcode0/ARCHITECTURE.md
@/home/alpine/vcode0/docs/README.md
@/home/alpine/vcode0/docs/operations/testing.md
@/home/alpine/vcode0/docs/foundations/newcomer.md
@/home/alpine/vcode0/docs/foundations/execution-flow.md
@/home/alpine/vcode0/docs/foundations/state-axes.md
@/home/alpine/vcode0/docs/foundations/coordination-rules.md
@/home/alpine/vcode0/test/integration/prompt-to-main-e2e.test.ts
@/home/alpine/vcode0/test/integration/verify-flake-audit.test.ts
@/home/alpine/vcode0/test/integration/tui/smoke.test.ts

<interfaces>
Key package contracts:

```json
"engines": { "node": ">=24" },
"scripts": {
  "check": "npm run check:fix; npm run format:check && npm run lint && npm run typecheck && npm run test",
  "test:tui:e2e": "command npx tui-test",
  "tui": "tsx src/main.ts",
  "postinstall": "node scripts/rebuild-node-pty.cjs"
}
```

Required README commands:

```bash
npm install
npm run tui
npm run tui -- --cwd /path/to/workspace
npm run check
npm run test:tui:e2e
```

Durable TUI startup text for dry-run proof:

```text
gvc0 progress
Run /init to create first milestone and planning feature.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root README source-install runbook</name>
  <files>README.md</files>
  <action>Create `/home/alpine/vcode0/README.md` because the root README is currently missing. Keep it concise and source-install focused: explain what gvc0 is, list prerequisites (Node.js 24+, npm, git, and native build tools for platforms that need the node-pty source rebuild), show `npm install`, show a minimal `gvc0.config.json` with the four model roles from the TUI smoke fixtures, show `npm run tui` and `npm run tui -- --cwd <workspace>`, document `npm run check` and `npm run test:tui:e2e`, and link to `ARCHITECTURE.md`, `docs/README.md`, `docs/foundations/newcomer.md`, and `docs/operations/testing.md`. Do not document public npm/global install as v1; distribution packaging is deferred to `REQ-DIST-V2-*`.</action>
  <verify>
    <automated>grep -n "npm install\|npm run tui\|gvc0.config.json" README.md</automated>
  </verify>
  <done>Root README exists and a fresh source user can follow it from install through TUI startup without reading internal planning files.</done>
</task>

<task type="auto">
  <name>Task 2: Verify README source-install runbook from a fresh checkout</name>
  <files>.planning/phases/12-integration-polish/12-03-SUMMARY.md</files>
  <action>Run a fresh source-install dry-run outside the active working tree. Prefer committing Task 1 first if required by local workflow, then clone with `git clone --no-local /home/alpine/vcode0 <tmp>/gvc0` so the command works across filesystems; otherwise use a clean source copy and record that it includes uncommitted README/planning changes. In the fresh checkout, run `npm install`. Create a temp workspace with `.gvc0/` and the minimal config from README. Launch `npm run tui -- --cwd <workspace>` and prove startup by observing `gvc0 progress` and `Run /init to create first milestone and planning feature.` Terminate the interactive process after startup proof. If the TUI fails to start, fix the root cause instead of weakening README claims.</action>
  <verify>
    <automated>timeout 900 bash -lc 'tmp=$(mktemp -d); git clone --no-local /home/alpine/vcode0 "$tmp/gvc0"; cd "$tmp/gvc0"; npm install; workspace=$(mktemp -d); mkdir -p "$workspace/.gvc0"; node -e '\''const fs=require("node:fs"),path=require("node:path");const workspace=process.argv[1];const config={models:{topPlanner:{provider:"anthropic",model:"claude-haiku-4-5"},featurePlanner:{provider:"anthropic",model:"claude-haiku-4-5"},taskWorker:{provider:"anthropic",model:"claude-haiku-4-5"},verifier:{provider:"anthropic",model:"claude-haiku-4-5"}}};fs.writeFileSync(path.join(workspace,"gvc0.config.json"),JSON.stringify(config,null,2)+"\n");'\'' "$workspace"; timeout 600 npm run test:tui:e2e'</automated>
  </verify>
  <done>12-03 summary records the dry-run location, commands, and startup evidence for `npm install && npm run tui` from fresh source.</done>
</task>

<task type="auto">
  <name>Task 3: Green-out v1 requirements traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>Update `.planning/REQUIREMENTS.md` so all v1 requirement checkboxes are resolved and the traceability table has no `Pending` statuses. For each v1 REQ-id, mark `[x]` only if v1 is complete and set the table status to `Complete — <concise evidence>`. If any requirement is not complete, keep the inventory honest by recording `v1.x follow-up — <explicit pointer/reason>` in the table. Use evidence from Phase 1-12 artifacts, docs, and tests; do not invent evidence. Preserve the v2 and out-of-scope sections.</action>
  <verify>
    <automated>! grep -n "| REQ-.*Pending\|- \[ \] \*\*REQ-" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>No v1 requirement remains pending; every row is complete with evidence or an explicit follow-up.</done>
</task>

<task type="auto">
  <name>Task 4: Finalize Phase 12 and milestone state</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/12-integration-polish/12-03-SUMMARY.md</files>
  <action>Create `12-03-SUMMARY.md` with exact dry-run and verification evidence, update `.planning/ROADMAP.md` to mark 12-03 and Phase 12 complete, and update `.planning/STATE.md` to record Phase 12/milestone completion. Include final traceability counts and cite 12-01, 12-02, and 12-03 as the full Phase 12 evidence chain.</action>
  <verify>
    <automated>npm run check && npm run test:tui:e2e</automated>
  </verify>
  <done>Roadmap/state reflect Phase 12 completion, 12-03 summary exists, and final verification is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| README command text → user shell | Fresh source users copy commands from README into a shell. |
| Fresh clone → npm postinstall | `npm install` executes project postinstall and native dependency rebuild logic. |
| README workspace path → TUI `--cwd` | User-provided workspace path controls where `.gvc0/state.db` and config are read/written. |
| Traceability table → milestone signoff | Requirement status controls whether v1 is considered complete or deferred. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-03-01 | Tampering | README shell snippets | mitigate | Use simple literal commands; avoid destructive cleanup snippets in README. |
| T-12-03-02 | Information Disclosure | README config example | mitigate | Use placeholder provider/model config only; do not include secrets or API keys. |
| T-12-03-03 | Denial of Service | `npm install` postinstall native rebuild | mitigate | Keep existing 12-02 postinstall non-fatal behavior and verify fresh install. |
| T-12-03-04 | Tampering | Dry-run workspace | mitigate | Use temp workspace outside repo and minimal config; do not reuse repo `.gvc0/state.db`. |
| T-12-03-05 | Repudiation | Requirements green-out | mitigate | Include evidence pointers per row and record final counts in summary. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source Type | Item | Status | Covered By |
|---|---|---|---|
| GOAL | Phase 12 SC4 source-install runbook verified by fresh-clone dry-run. | COVERED | Task 1 + Task 2. |
| GOAL | Phase 12 SC5 all v1 REQ-ids complete or explicit follow-up. | COVERED | Task 3. |
| CONTEXT | 12-01 remains backend lifecycle proof. | COVERED | Task 3 evidence and Task 4 summary. |
| CONTEXT | 12-02 remains TUI golden-path proof. | COVERED | Task 3 evidence and Task 4 summary. |
| RESEARCH | Root README missing. | COVERED | Task 1 creates it. |
| RESEARCH | `npm install` must exercise postinstall. | COVERED | Task 2. |
| RESEARCH | TUI startup proof should not require live provider calls. | COVERED | Task 2 minimal startup proof. |
| REQUIREMENTS | `.planning/REQUIREMENTS.md` all pending today. | COVERED | Task 3. |
| VALIDATION | Final gate must include Vitest and TUI E2E lanes. | COVERED | Task 4 verification. |
</source_audit>

<verification>
Focused checks:

```bash
grep -n "npm install\|npm run tui\|gvc0.config.json" README.md
! grep -n "| REQ-.*Pending\|- \[ \] \*\*REQ-" .planning/REQUIREMENTS.md
npm run test:tui:e2e
```

Fresh source-install dry-run:

```bash
npm install
npm run tui -- --cwd <temp-workspace>
```

Final gate:

```bash
npm run check && npm run test:tui:e2e
```
</verification>

<success_criteria>
- `README.md` exists and contains the source-install runbook.
- Fresh source checkout/copy dry-run runs `npm install` and reaches TUI startup text via `npm run tui`.
- `.planning/REQUIREMENTS.md` contains no pending v1 traceability rows.
- `.planning/ROADMAP.md` marks 12-03 and Phase 12 complete.
- `.planning/STATE.md` records final Phase 12/milestone completion.
- `npm run check && npm run test:tui:e2e` passes.
</success_criteria>

<output>
After completion, create `/home/alpine/vcode0/.planning/phases/12-integration-polish/12-03-SUMMARY.md` with:

- root README contents summary;
- exact source-install dry-run commands and observed startup text;
- final v1 traceability counts and any follow-up rows;
- verification commands/results;
- roadmap/state updates;
- milestone completion handoff.
</output>
