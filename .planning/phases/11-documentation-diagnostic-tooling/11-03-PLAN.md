---
phase: 11-documentation-diagnostic-tooling
plan: 03
type: execute
wave: 1
depends_on:
  - 11-02
files_modified:
  - docs/concerns/README.md
  - docs/concerns/destructive-ops-non-git.md
  - docs/concerns/merge-train-reentry-cap.md
  - docs/concerns/merge-train-reentry-starvation.md
  - docs/concerns/planner-write-reservation-accuracy.md
  - docs/concerns/summarize-retry-loop.md
  - docs/concerns/verification-and-repair-churn.md
  - docs/concerns/worker-runaway.md
  - docs/operations/testing.md
  - docs/foundations/newcomer.md
  - .planning/phases/11-documentation-diagnostic-tooling/11-03-SUMMARY.md
autonomous: true
requirements:
  - REQ-DOC-01
  - REQ-DOC-02
  - REQ-DOC-03
must_haves:
  truths:
    - "Every current `docs/concerns/*.md` page cross-links to at least one executable test or explicitly labels the mitigation as deferred/no-direct-coverage."
    - "The testing operations reference contains one concerns-to-tests map so operators can see which risks are guarded by executable tests and which remain watchpoints."
    - "The newcomer walkthrough remains the single end-to-end narrative from prompt to `main`; update it in place rather than creating a duplicate narrative doc."
    - "11-03 does not implement new runtime safeguards or new executable tests unless a doc claim would otherwise be false; it is a traceability and narrative slice."
  artifacts:
    - path: "docs/operations/testing.md"
      provides: "Concerns-to-tests map with coverage status and deferred gaps"
      contains: "Concerns-to-tests traceability"
    - path: "docs/foundations/newcomer.md"
      provides: "Updated newcomer end-to-end narrative from prompt to merged main commit"
      contains: "Prompt → main"
    - path: "docs/concerns/README.md"
      provides: "Concern index that points to the testing traceability map"
      contains: "testing traceability"
    - path: ".planning/phases/11-documentation-diagnostic-tooling/11-03-SUMMARY.md"
      provides: "Implementation summary and Phase 12 handoff"
      contains: "11-03 Summary"
---

<objective>
Close Phase 11's remaining documentation criteria by connecting each concern page to executable proof where proof exists, labeling deferred gaps honestly, and refreshing the newcomer narrative so a reader can follow one prompt through planning, execution, verification, merge train, inbox waits, diagnostics, and final merge without reading code.

This is a documentation/traceability slice. It should update existing docs rather than create a second concern catalog or duplicate newcomer walkthrough.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-documentation-diagnostic-tooling/11-02-SUMMARY.md

@docs/concerns/README.md
@docs/concerns/destructive-ops-non-git.md
@docs/concerns/merge-train-reentry-cap.md
@docs/concerns/merge-train-reentry-starvation.md
@docs/concerns/planner-write-reservation-accuracy.md
@docs/concerns/summarize-retry-loop.md
@docs/concerns/verification-and-repair-churn.md
@docs/concerns/worker-runaway.md
@docs/operations/testing.md
@docs/operations/warnings.md
@docs/foundations/newcomer.md
@docs/foundations/execution-flow.md
@docs/foundations/state-axes.md
@docs/foundations/coordination-rules.md
@docs/reference/tui.md

<interfaces>
Current concern pages:
- `docs/concerns/destructive-ops-non-git.md`
- `docs/concerns/merge-train-reentry-cap.md`
- `docs/concerns/merge-train-reentry-starvation.md`
- `docs/concerns/planner-write-reservation-accuracy.md`
- `docs/concerns/summarize-retry-loop.md`
- `docs/concerns/verification-and-repair-churn.md`
- `docs/concerns/worker-runaway.md`

Likely executable coverage discovered during planning:
- destructive ops: `test/unit/agents/destructive-ops.test.ts`, `test/integration/destructive-op-approval.test.ts`
- merge-train re-entry cap: `test/unit/core/merge-train.test.ts`, `test/integration/merge-train.test.ts`, `test/unit/core/warnings.test.ts`
- merge-train re-entry starvation: `test/unit/core/merge-train.test.ts`, `test/unit/core/warnings.test.ts`
- planner write reservations: `test/unit/orchestrator/verify-repairs.test.ts`, `test/unit/core/proposals.test.ts`, `test/unit/orchestrator/scheduler-loop.test.ts`
- summarize retry loop: `test/unit/orchestrator/summaries.test.ts`, `test/unit/orchestrator/scheduler-loop.test.ts`
- verification and repair churn: `test/unit/orchestrator/verify-repairs.test.ts`, `test/integration/feature-lifecycle-e2e.test.ts`, `test/unit/core/warnings.test.ts`, `specs/test_feature_verification_repair_loop.md`
- worker runaway: `test/integration/worker-smoke.test.ts` only covers runtime bootstrap; wall-clock budget, progress-idle timeout, and cost cutoff remain deferred/no-direct-coverage.
</interfaces>

<scope_guardrails>
- Do not implement the Phase 12 end-to-end scripted scenario.
- Do not add a new documentation site structure or duplicate `docs/foundations/newcomer.md`.
- Do not claim executable coverage for deferred risks; mark them as deferred/no-direct-coverage.
- Do not add new runtime guardrails for worker runaway or destructive non-git ops in this plan.
- Do not edit source code unless needed to fix a broken doc link discovered by verification.
</scope_guardrails>
</context>

<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Add concern-to-test traceability to the testing reference and concern index</name>
  <files>docs/operations/testing.md, docs/concerns/README.md</files>
  <behavior>
    - Add a `Concerns-to-tests traceability` section to `docs/operations/testing.md`.
    - Include one row per concern page with columns for concern, executable proof, coverage status, and remaining gap.
    - Use exact repo-relative paths for test files and specs.
    - Mark `worker-runaway` and non-git destructive operation expansion honestly as deferred/no-direct-coverage where appropriate.
    - Update `docs/concerns/README.md` to point readers to the traceability section.
  </behavior>
  <verify>
    <automated>npm run format:check && npm run lint</automated>
  </verify>
  <done>
    A reader can start from either the concerns index or testing reference and find the executable proof/deferred status for every current concern page.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cross-link each concern page to its executable proof or deferred gap</name>
  <files>docs/concerns/*.md</files>
  <behavior>
    - Add a concise `Executable coverage` or equivalent section to each concern page.
    - Link to the specific test files/specs identified in Task 1.
    - For concerns with partial coverage, distinguish shipped proof from open watchpoint.
    - For `worker-runaway.md`, state that `worker-smoke.test.ts` proves runtime bootstrap only and that timeout/cost cutoff coverage remains deferred.
    - Keep existing `Concern`, `Why to Watch`, `What to Observe`, `Current Position`, and `Related` content intact unless it is contradicted by shipped status.
  </behavior>
  <verify>
    <automated>npm run format:check && npm run lint</automated>
  </verify>
  <done>
    Every current concern doc links to executable proof or explicitly states no direct mitigation test exists yet.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refresh the newcomer prompt-to-main narrative</name>
  <files>docs/foundations/newcomer.md</files>
  <behavior>
    - Update the existing newcomer walkthrough in place.
    - Keep it as one end-to-end story from prompt to `main`.
    - Add the shipped Phase 8–11 surfaces where they help orientation: inbox overlay, merge-train overlay, transcript/config surfaces, planner audit/proposal review, and `gvc0 explain` diagnostics.
    - Correct stale implementation details if found; for example avoid claiming task rows store commit SHAs if current data model does not.
    - Link to canonical details instead of duplicating FSM/coordination tables.
  </behavior>
  <verify>
    <automated>npm run format:check && npm run lint</automated>
  </verify>
  <done>
    A new reader can follow the current shipped flow without reading orchestrator code and can branch into canonical state/execution/coordination references when needed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Summarize 11-03 and close Phase 11 planning state</name>
  <files>.planning/phases/11-documentation-diagnostic-tooling/11-03-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <behavior>
    - Write `11-03-SUMMARY.md` with the final concern/test mapping, docs updated, deferred gaps, and verification results.
    - Mark 11-03 complete in `.planning/ROADMAP.md` and update Phase 11 progress/state in `.planning/STATE.md`.
    - Hand off clearly to Phase 12 integration/polish work.
  </behavior>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    Phase 11 is marked complete or ready for completion according to the roadmap, with Phase 12 as the next autonomous target.
  </done>
</task>
</tasks>

<verification>
Focused checks while editing docs:

```bash
npm run format:check
npm run lint
```

Final gate before commit:

```bash
npm run check
```
</verification>
