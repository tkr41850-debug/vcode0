---
phase: 05-feature-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - test/integration/feature-lifecycle-e2e.test.ts
  - test/helpers/feature-lifecycle-fixture.ts
  - test/unit/core/fsm/feature-boundary-guards.test.ts
autonomous: true
requirements:
  - REQ-PLAN-02
  - REQ-MERGE-04
must_haves:
  truths:
    - "A new integration test walks planning → executing → ci_check → verifying → awaiting_merge end-to-end using LocalWorkerPool + faux planner + faux executor + faux verifier"
    - "The walk asserts feature reaches workControl='awaiting_merge' with collabControl='branch_open'"
    - "FSM guard `verifying → awaiting_merge` is unit-tested to require collabControl='branch_open' (explicit assertion, not just incidental pass)"
    - "FSM guard `awaiting_merge → work_complete` is unit-tested to require the feature to be merged (status marker or collabControl progression)"
    - "A reusable fixture helper `createFeatureLifecycleFixture()` exists under test/helpers/ and is consumed by this plan and referenced by 05-03 and 05-04"
  artifacts:
    - path: "test/integration/feature-lifecycle-e2e.test.ts"
      provides: "End-to-end happy-path walk of Phase 5 FSM"
      contains: "describe('feature lifecycle e2e"
    - path: "test/helpers/feature-lifecycle-fixture.ts"
      provides: "Reusable fixture: real-git tmpDir + LocalWorkerPool + faux agents + minimal feature+task DAG seed"
      exports: ["createFeatureLifecycleFixture"]
    - path: "test/unit/core/fsm/feature-boundary-guards.test.ts"
      provides: "Explicit unit tests on verifying→awaiting_merge and awaiting_merge→work_complete guards"
      contains: "validateFeatureWorkTransition"
  key_links:
    - from: "test/integration/feature-lifecycle-e2e.test.ts"
      to: "test/helpers/feature-lifecycle-fixture.ts::createFeatureLifecycleFixture"
      via: "import { createFeatureLifecycleFixture }"
      pattern: "createFeatureLifecycleFixture"
    - from: "createFeatureLifecycleFixture"
      to: "src/runtime/worker-pool.ts::LocalWorkerPool"
      via: "harness wires real worker pool around tmp git repo"
      pattern: "LocalWorkerPool|InProcessHarness"
    - from: "createFeatureLifecycleFixture"
      to: "src/orchestrator/features/index.ts::FeatureLifecycleCoordinator"
      via: "scheduler loop drives phase transitions through coordinator"
      pattern: "completePhase|advancePhase"
---

<objective>
Build an end-to-end integration test that walks Phase 5's happy-path FSM (planning → executing → ci_check → verifying → awaiting_merge) using a real `LocalWorkerPool` over a tmp git worktree plus faux planner / executor / verifier transcripts. Also, explicitly unit-test the two boundary FSM guards — `verifying → awaiting_merge` (requires `collabControl=branch_open`) and `awaiting_merge → work_complete` (requires merged state). Extract a reusable fixture helper so 05-03 and 05-04 can share it.

Purpose: Satisfies SC2 ("Feature lifecycle transitions through planning → executing → ci_check → verifying → awaiting_merge, validated by FSM guards"). Per RESEARCH, FSM guards are already complete in `src/core/fsm/index.ts:180-293`; this plan adds integration coverage proving they work under real event-queue traffic, and pins the two boundary transitions with explicit unit tests. The extracted fixture unblocks 05-03 (empty-diff verify) and 05-04 (repair-loop E2E).

Output:
- New integration test `test/integration/feature-lifecycle-e2e.test.ts` walking the full happy path.
- New fixture helper `test/helpers/feature-lifecycle-fixture.ts` exporting `createFeatureLifecycleFixture()`.
- New unit test `test/unit/core/fsm/feature-boundary-guards.test.ts` for the two boundary transitions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-feature-lifecycle/05-CONTEXT.md
@.planning/phases/05-feature-lifecycle/05-RESEARCH.md
@.planning/phases/05-feature-lifecycle/05-PATTERNS.md
@.planning/phases/05-feature-lifecycle/05-01-SUMMARY.md

@src/core/fsm/index.ts
@src/orchestrator/features/index.ts
@src/orchestrator/scheduler/dispatch.ts
@src/runtime/worker-pool.ts
@test/integration/feature-phase-agent-flow.test.ts
@test/integration/worker-retry-commit.test.ts
@test/integration/harness/in-process-harness.ts

<interfaces>
<!-- Extracted from codebase. Executor should use these directly. -->

From src/core/fsm/index.ts:
- `validateFeatureWorkTransition(from: WorkControl, to: WorkControl, ctx: { collabControl, repairAttempts, ... }) -> { ok: true } | { ok: false, reason: string }`
- Key boundary rules (per RESEARCH lines 180-293):
  - `verifying → awaiting_merge` requires `ctx.collabControl === 'branch_open'`.
  - `awaiting_merge → work_complete` requires the feature's merge state to indicate merged (check exact field name in fsm/index.ts during impl — likely `ctx.merged === true` or `ctx.collabControl === 'merged'`).
  - `verifying → executing_repair` (used by 05-04) requires repairAttempts < MAX_REPAIR_ATTEMPTS.

From src/orchestrator/features/index.ts (FeatureLifecycleCoordinator):
- `completePhase(featureId, phase, result)` — happy path transitions via `markPhaseDone` + `advancePhase`.
- `onTaskLanded(featureId)` — called from scheduler `result` handler (events.ts:171-204) when a task's `task_complete(submitted)` arrives. Triggers ci_check dispatch when all tasks in the feature are done.

From src/runtime/worker-pool.ts:
- `LocalWorkerPool` constructor takes `{ agentFactory, transport, workerSpawner, ... }`. For tests, `InProcessHarness` from `test/integration/harness/in-process-harness.ts` wraps it with in-process workers driven by faux agents.

From test/integration/worker-retry-commit.test.ts (beforeEach setup, lines 1-92):
- `spawnSync('git', ['init', '-q'], { cwd: tmpDir })`
- `spawnSync('git', ['config', 'user.email', 'test@test'], {cwd: tmpDir})` + `user.name`
- `spawnSync('git', ['config', 'commit.gpgsign', 'false'], {cwd: tmpDir})`
- Seed commit: write a file, `git add . && git commit -m "seed"`.
- afterEach: `fs.rmSync(tmpDir, { recursive: true, force: true })`.

From test/integration/feature-phase-agent-flow.test.ts (lines 247-331):
- `createFixture()` wires `PiFeatureAgentRuntime` + `InMemoryStore` + `SchedulerLoop`. For E2E, this plan must extend it with a real `LocalWorkerPool` pointing at the tmp git repo.
- Faux transcript patterns: `fauxAssistantMessage([fauxToolCall(...)], { stopReason: 'toolUse' })`.

From src/orchestrator/scheduler/dispatch.ts:237-365 (dispatch routing):
- `plan` → `agents.planFeature`
- `ci_check` → shell (permissive when `verification.feature.checks = []`)
- `verify` → `agents.verifyFeature`
- `summarize` → `agents.summarizeFeature`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract reusable feature-lifecycle fixture helper</name>
  <files>test/helpers/feature-lifecycle-fixture.ts</files>
  <action>
    Create a new helper that returns: `{ tmpDir, git-init-artifacts, graph, ports, store, sessionStore, pool: LocalWorkerPool, runtime: PiFeatureAgentRuntime, scheduler: SchedulerLoop, faux (per-phase faux providers), seedFeature(featureId, {description, successCriteria, tasks: string[]}) }`.

    Implementation sketch:
    1. Use `os.tmpdir()` + `fs.mkdtempSync` to allocate the test's tmp repo. Run `git init`, set user/email/commit.gpgsign, seed one commit (copy beforeEach from `worker-retry-commit.test.ts:1-92`).
    2. Build `InMemoryStore`, `InMemorySessionStore`, `OrchestratorPorts` (mirror `feature-phase-agent-flow.test.ts::createFixture` lines 247-294).
    3. Register three faux providers keyed by role (planner, executor, verifier). Accept a `{ planner?, executor?, verifier? }` options object so callers can script each independently.
    4. Wire `LocalWorkerPool` + `InProcessHarness` (copy from `worker-retry-commit.test.ts`). The executor worker uses the executor faux provider.
    5. `seedFeature(featureId, opts)` writes a minimal `Feature` record via `graph.addFeature(...)` and (optionally) pre-adds tasks. Default is zero tasks (planner creates them) so the helper supports both "start from plan phase" and "skip plan and seed tasks" modes.
    6. Return a `teardown()` function that closes the pool + rm's tmpDir.

    CRITICAL: Do NOT couple the helper to verify-agent specifics — 05-03 needs it with different verifier scripts and 05-04 with a failing-then-passing verifier transcript. Keep the faux wiring generic.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Helper exists under `test/helpers/`, exports `createFeatureLifecycleFixture`, passes typecheck, compiles against existing InProcessHarness + LocalWorkerPool types. Not yet consumed by tests (Task 2 does that).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end happy-path integration test</name>
  <files>test/integration/feature-lifecycle-e2e.test.ts</files>
  <behavior>
    - Script planner: `addTask('implement X')`, `addTask('wire X into Y')`, `addDependency('wire X into Y' → 'implement X')`, `submit`.
    - Script executor: for each task, emit `claim_lock` → `commit_done(sha, trailerOk: true)` → `task_complete(submitted)`. Use the shared tmp git repo — a real commit must be produced per task so trailer observation can round-trip.
    - Script verifier: on first invocation, emit `submitVerify({outcome: 'pass'})` (no raiseIssue). No repair-loop involvement in this happy-path test.
    - Assert progression (after scheduler drains to idle):
      - `graph.features.get('f-1')` matches `{ workControl: 'awaiting_merge', status: 'pending', collabControl: 'branch_open' }`.
      - Each task has `status: 'done'`.
      - One `AgentRun` row per phase (plan, ci_check, verify) with `runStatus: 'completed'`.
      - `store.appendEvent` log contains the phase-complete events in order: `feature_phase_complete(plan)`, task landings, `feature_phase_complete(ci_check)`, `feature_phase_complete(verify)`.
  </behavior>
  <action>
    Import `createFeatureLifecycleFixture` from Task 1. Build the three faux transcripts. Call `scheduler.runUntilIdle()` (or equivalent loop-drain helper). Assert as above.

    Do NOT stress the repair loop — that is 05-04. Do NOT test empty-diff — that is 05-03. Keep this test focused on the happy path; one pass, clean `awaiting_merge` terminus.

    If the ci_check permissive config is not the default, explicitly pass `verification.feature.checks = []` through the fixture's config hook.
  </action>
  <verify>
    <automated>npx vitest run test/integration/feature-lifecycle-e2e.test.ts -t "happy path"</automated>
  </verify>
  <done>Test passes. Feature reaches `awaiting_merge` with `collabControl='branch_open'`. Per-phase AgentRun rows recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Boundary-guard unit tests</name>
  <files>test/unit/core/fsm/feature-boundary-guards.test.ts</files>
  <behavior>
    - Test 1: `validateFeatureWorkTransition('verifying', 'awaiting_merge', { collabControl: 'branch_open', ... })` → `{ ok: true }`.
    - Test 2: `validateFeatureWorkTransition('verifying', 'awaiting_merge', { collabControl: 'none', ... })` → `{ ok: false, reason: /branch_open/i }` (exact reason string pinned after reading fsm/index.ts).
    - Test 3: `validateFeatureWorkTransition('awaiting_merge', 'work_complete', { /* merged=true or collabControl='merged' */ })` → `{ ok: true }`.
    - Test 4: `validateFeatureWorkTransition('awaiting_merge', 'work_complete', { /* NOT merged */ })` → `{ ok: false, reason: /merge/i }`.
  </behavior>
  <action>
    Read `src/core/fsm/index.ts:180-293` first to confirm the exact context field names (`collabControl`, and the merged marker — likely `collabControl: 'merged'` or a separate `merged?: boolean`). Write the four tests using the real field names, not guesses. Cover both directions of each boundary (pass + fail). Place file under `test/unit/core/fsm/` to match existing fsm test layout.
  </action>
  <verify>
    <automated>npx vitest run test/unit/core/fsm/feature-boundary-guards.test.ts</automated>
  </verify>
  <done>Four unit tests pass. Boundary guards are pinned by explicit assertions; future regressions on these two transitions fail the unit suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler event queue → FSM guards | Every feature-level transition must pass `validateFeatureWorkTransition` before `advancePhase` fires. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | FSM regression allows `verifying → awaiting_merge` without `branch_open` | mitigate | Task 3 unit test asserts the boundary guard in both directions. |
| T-05-02-02 | Tampering | Scheduler skips ci_check or verify phase on happy path | mitigate | Task 2 E2E asserts each phase's `feature_phase_complete` event in order. |
| T-05-02-03 | Denial of Service | LocalWorkerPool leaves tmpDir behind on test failure | accept | afterEach teardown handles the normal case; CI cleans up `/tmp` between runs. |
</threat_model>

<verification>
- `npx vitest run test/integration/feature-lifecycle-e2e.test.ts` — happy path passes, feature reaches `awaiting_merge`.
- `npx vitest run test/unit/core/fsm/feature-boundary-guards.test.ts` — four boundary tests pass.
- `npm run test:integration` — no regressions in the broader integration suite.
- `npm run check` — no lint/format/type regressions.
- Manual: `test/helpers/feature-lifecycle-fixture.ts` exports `createFeatureLifecycleFixture` with a signature suitable for 05-03 (empty-diff verify) and 05-04 (repair-loop E2E).
</verification>

<success_criteria>
**Maps to ROADMAP SC2 (REQ-PLAN-02, REQ-MERGE-04 initial):**
- [x] Integration test proves full happy-path walk: planning → executing → ci_check → verifying → awaiting_merge.
- [x] FSM guards validated under real event-queue traffic (not just unit isolation).
- [x] Boundary transitions (`verifying → awaiting_merge`, `awaiting_merge → work_complete`) pinned by explicit unit tests.
- [x] Reusable fixture helper extracted for 05-03 and 05-04.

**Scope Fence (NOT this plan):**
- Repair loop (verify fail → executing_repair → verify pass) — 05-04.
- Empty-diff verify handling — 05-03.
- Changed-files git integration in verify prompt — 05-03.
- Hallucinated-progress gate — 05-04.
- Merge-train (awaiting_merge → integrating → merged) — Phase 6.
- Summarize phase deep-dive — existing surface, not re-tested here beyond incidental pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-feature-lifecycle/05-02-SUMMARY.md`. Include:
- New test file paths + pass states.
- `createFeatureLifecycleFixture` signature (for 05-03 / 05-04 to consume directly).
- Any fsm-guard details discovered during Task 3 that surprise (e.g., exact field name for "merged" marker) — downstream phases depend on those.
</output>
