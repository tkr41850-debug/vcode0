---
phase: 05-feature-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/feature-phase-agent-flow.test.ts
  - test/unit/orchestrator/proposals.test.ts
  - src/agents/prompts/plan.ts
autonomous: true
requirements:
  - REQ-PLAN-02
must_haves:
  truths:
    - "AgentRuntime.planFeature() produces graph mutations from a scripted faux transcript (addTask x N + addDependency + submit)"
    - "A cycle-creating addDependency lands in applyGraphProposal.skipped[] with a visible GraphValidationError reason (planner does not crash)"
    - "Duplicate addTask calls follow a deterministic, documented convention (reject as skipped[] — chosen for consistency with existing GraphValidationError path)"
    - "submit-before-any-addTask surfaces a protocol error (either skipped[] with reason or explicit agent-side error — covered by a test either way)"
    - "editTask({patch:{weight}}) reweight path is exercised end-to-end by at least one faux transcript step"
    - "Plan prompt input contract is documented inline in src/agents/prompts/plan.ts (what summaries/context it sees)"
  artifacts:
    - path: "test/integration/feature-phase-agent-flow.test.ts"
      provides: "New 'plan phase emits task DAG via typed tools' + edge-case cases (cycle, duplicate, submit-before-task, reweight)"
      contains: "describe('plan phase acceptance'"
    - path: "test/unit/orchestrator/proposals.test.ts"
      provides: "Unit-level assertions on applyGraphProposal skipped[] for cycle and duplicate addTask"
      contains: "skipped"
    - path: "src/agents/prompts/plan.ts"
      provides: "Documented input contract comment block at top of PLAN_PROMPT"
      contains: "Input Contract"
  key_links:
    - from: "test/integration/feature-phase-agent-flow.test.ts::plan phase acceptance"
      to: "src/agents/runtime.ts::planFeature"
      via: "createFixture + faux transcript + scheduler loop step"
      pattern: "planFeature|runProposalPhase"
    - from: "planFeature transcript"
      to: "src/orchestrator/proposals/index.ts::applyGraphProposal"
      via: "proposal apply path via scheduler event queue"
      pattern: "applyGraphProposal|skipped"
---

<objective>
Prove `AgentRuntime.planFeature()` end-to-end with a scripted faux planner transcript that addTask x N, addDependency, (optional reweight), submit. Harden proposal-tool edge cases: cycle-creating addDependency must land in skipped[] with a reason, duplicate addTask must follow a deterministic convention, and submit-before-any-addTask must error (not silently succeed with an empty graph). Document the planner prompt input contract inside `src/agents/prompts/plan.ts`.

Purpose: Satisfies REQ-PLAN-02 SC1 acceptance surface — "Feature-level planner emits a task DAG via typed pi-sdk tool calls." All building blocks already exist on main (planner agent, proposal tools, apply path, dispatch wiring). Phase 5 closes the test-coverage gap and documents the contract so downstream phases (Phase 7 top-level planner) can rely on it.

Output:
- Faux-backed integration cases in `feature-phase-agent-flow.test.ts` (under a new `describe('plan phase acceptance')`).
- Unit-level regression on `applyGraphProposal` for cycle / duplicate addTask / empty submit.
- Documentation comment block at the top of `PLAN_PROMPT` enumerating what summaries the planner sees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-feature-lifecycle/05-CONTEXT.md
@.planning/phases/05-feature-lifecycle/05-RESEARCH.md
@.planning/phases/05-feature-lifecycle/05-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@src/agents/runtime.ts
@src/agents/prompts/plan.ts
@src/agents/tools/schemas.ts
@src/orchestrator/proposals/index.ts
@src/orchestrator/scheduler/dispatch.ts
@test/integration/feature-phase-agent-flow.test.ts

<interfaces>
<!-- Extracted from codebase. Executor should use these directly; no exploration needed. -->

From src/agents/runtime.ts (AgentRuntime phase entry points):
- `planFeature(featureId: FeatureId, opts?): Promise<...>` — dispatches `runProposalPhase('plan', featureId, ...)`, returns when `submit` tool is called (or throws if not). Hosted tools: proposal tools from `featurePhaseToolParameters` + `proposalToolParameters`.
- `runProposalPhase(phase, featureId, ...)` threads `successCriteria`, `planSummary`, `executionEvidence`, `verificationResults`, `decisions` into `renderPrompt`.

From src/agents/tools/schemas.ts (proposal tools — typebox):
- `proposalToolParameters.addTask = Type.Object({ featureId: Type.String(), description: Type.String(), weight?: ..., reservedWritePaths?: ... })`
- `proposalToolParameters.addDependency = Type.Object({ featureId: Type.String(), from: Type.String(), to: Type.String() })` (aliases allowed)
- `proposalToolParameters.editTask = Type.Object({ taskId: Type.String(), patch: Type.Object({ weight?: ..., description?: ..., reservedWritePaths?: ... }) })`
- `proposalToolParameters.submit = Type.Object({})`

From src/orchestrator/proposals/index.ts:
- `applyGraphProposal(graph, proposal) -> { applied[], skipped[] }` where `skipped[]` entries are `{ op, reason: string, kind: 'GraphValidationError' | ... }`.
- Cycle rejection happens inside `graph.addDependency` → `GraphValidationError` → caught and pushed to `skipped[]` with its message as reason.

From test/integration/feature-phase-agent-flow.test.ts (existing fixture shape lines 247-331):
- `createFixture()` wires `PiFeatureAgentRuntime`, `InMemoryStore`, `InMemorySessionStore`, `OrchestratorPorts`, `SchedulerLoop`.
- `faux.setResponses([...])` scripts the planner agent.
- Use `fauxAssistantMessage([fauxToolCall('addTask', {...}), fauxToolCall('submit', {})], { stopReason: 'toolUse' })` for a one-shot transcript.

From src/agents/prompts/plan.ts:
- Exports `PLAN_PROMPT` string + label list consumed by `renderPrompt`. This plan adds a documentation comment at the top describing the input contract (what `renderPrompt` threads into it from the feature record).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Faux-backed planner acceptance integration case</name>
  <files>test/integration/feature-phase-agent-flow.test.ts</files>
  <behavior>
    - In a new `describe('plan phase acceptance', ...)` block, add a test that scripts a faux planner transcript with: `addTask({featureId, description: 'build X'})`, `addTask({featureId, description: 'wire Y'})`, `addDependency({featureId, from: 'wire Y', to: 'build X'})`, `editTask({taskId: <resolved>, patch: {weight: 'large'}})`, `submit({})`. After the scheduler loop steps, assert: (a) graph.tasks has two new tasks on the feature, (b) `graph.edges` or dependency accessor reflects the wire-Y → build-X dep, (c) the reweighted task has `weight: 'large'`, (d) feature `workControl === 'executing'` (plan phase complete → advanced), (e) one `AgentRun` row exists for phase='plan' with runStatus='completed'.
    - Follow existing `createFixture()` pattern (lines 247-294) for wiring; follow existing faux transcript pattern (lines 299-331).
  </behavior>
  <action>
    Add the new `describe('plan phase acceptance')` block near the top of the file. Use `createFixture()` as-is. Script the transcript with `fauxAssistantMessage([fauxToolCall('addTask', ...), fauxToolCall('addTask', ...), fauxToolCall('addDependency', ...), fauxToolCall('editTask', ...), fauxToolCall('submit', {})], { stopReason: 'toolUse' })` followed by `fauxAssistantMessage([fauxText('Planning complete.')])`. For `editTask.taskId` — if the taskId is not known at script-time (addTask returns a generated id), use the dep-target alias resolution via `{taskId: 'build X'}` (per schemas.ts, `editTask.taskId` accepts aliases from the current proposal batch). Assertion pattern copies lines 341-355. Do NOT touch merge-train / verify / repair test cases (Phase 6 / 05-04).
  </action>
  <verify>
    <automated>npx vitest run test/integration/feature-phase-agent-flow.test.ts -t "plan phase acceptance"</automated>
  </verify>
  <done>New integration case passes; asserts task DAG mutation + reweight + plan-phase completion; uses only public fixture surface (no runtime internals).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proposal-tool edge-case unit tests (cycle, duplicate addTask, submit-before-task)</name>
  <files>test/unit/orchestrator/proposals.test.ts</files>
  <behavior>
    - Test 1 (cycle): Given a feature with tasks A and B and an existing dep A→B, a proposal batch `[addDependency(B→A), submit]` applied via `applyGraphProposal` must yield `skipped[]` containing the B→A op with a reason matching `/cycle|GraphValidationError/i`. The pre-existing A→B dep remains; no B→A dep is added.
    - Test 2 (duplicate addTask): Two `addTask` ops in the same batch with identical `featureId` + `description` — the second op must land in `skipped[]` with a reason indicating duplicate (the convention chosen: reject the duplicate; the first is applied). This matches existing `GraphValidationError` semantics for uniqueness; confirm by inspecting `graph.addTask` in `src/core/graph/index.ts` during implementation. If the current implementation is idempotent (same-alias returns existing id), document that outcome in the test instead and assert idempotent behavior — pick whichever the code actually does and lock it with a test.
    - Test 3 (submit-before-addTask): A proposal batch containing only `[submit]` on a feature with zero existing tasks — applying it must NOT silently succeed as "empty plan". Expectation: `submit` is recorded but the feature's plan phase does not advance, OR `applied[]` is empty with a `skipped[]` entry. Exact assertion depends on `applyGraphProposal` semantics; the test pins whichever is current truth.
  </behavior>
  <action>
    Create (or extend if it exists) `test/unit/orchestrator/proposals.test.ts`. Import `applyGraphProposal` and a test-helper graph builder. If no such helper exists, hand-build a minimal `FeatureGraph` fixture inline (follow the pattern used by `test/unit/orchestrator/scheduler-loop.test.ts` for in-memory graph setup). Before writing assertions, run a quick `grep -n "class.*GraphValidationError\|addTask\|addDependency" src/core/graph/index.ts` to confirm exact error message shape; assert against that shape. If the file already exists and has overlapping cases, add the three new cases under a new `describe('edge cases', ...)` block rather than editing existing cases.
  </action>
  <verify>
    <automated>npx vitest run test/unit/orchestrator/proposals.test.ts</automated>
  </verify>
  <done>Three new unit tests pass. Each pins a deterministic outcome for the edge case. Duplicate-addTask convention (reject vs idempotent) is locked by a test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document planner prompt input contract</name>
  <files>src/agents/prompts/plan.ts</files>
  <behavior>
    - Add a JSDoc/TS-comment block at the top of `PLAN_PROMPT` export (above the string constant) listing each input field threaded by `renderPrompt` (from `src/agents/runtime.ts:271-324`): `featureDescription`, `successCriteria`, `planSummary` (if prior iteration), `executionEvidence` (empty on first plan), `verificationResults`, `decisions`. For each field, note: (a) source (feature record / prior phase summary), (b) whether it is set on first-plan vs replan, (c) the labeled block name it renders under in the final prompt.
    - No behavior change; pure documentation. After edit, `npm run typecheck` still passes.
  </behavior>
  <action>
    Read `src/agents/runtime.ts:271-324` (`renderPrompt`) and `src/agents/prompts/plan.ts` in full. Write a `/** Input Contract ... */` block above `PLAN_PROMPT`. Keep the block <= 40 lines. Do NOT modify the prompt string itself. Do NOT document fields that `renderPrompt` does not thread into the `plan` phase.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>`plan.ts` has an `Input Contract` comment block enumerating all `renderPrompt`-threaded fields. `npm run check` passes (no formatting regressions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner agent (LLM) → proposal apply | Planner-emitted tool calls cross into orchestrator graph mutation via typed tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Planner emits cycle-creating addDependency to corrupt DAG | mitigate | `applyGraphProposal` catches `GraphValidationError`, pushes op to `skipped[]`; Task 2 locks this with a unit test. |
| T-05-01-02 | Tampering | Planner emits duplicate addTask to inflate the graph | mitigate | Task 2 pins convention (reject or idempotent) with a unit test; graph-layer uniqueness check holds. |
| T-05-01-03 | Denial of Service | Planner submits empty plan (no addTask + submit) causing silent plan-phase completion | mitigate | Task 2 asserts current semantics; if the code accepts empty plans, raise as a deferred concern for Phase 7 (top-level planner invoking replan). |
| T-05-01-04 | Input Validation | Planner tool call has malformed shape | mitigate | Existing typebox schemas on `proposalToolParameters` reject malformed calls before dispatch; no new mitigation required. |
</threat_model>

<verification>
- `npx vitest run test/integration/feature-phase-agent-flow.test.ts -t "plan phase acceptance"` — new acceptance case passes.
- `npx vitest run test/unit/orchestrator/proposals.test.ts` — three edge-case tests pass.
- `npm run check` — no lint/type/format regressions.
- Manual: Open `src/agents/prompts/plan.ts` and confirm the `Input Contract` block is present and accurate against `renderPrompt` (lines 271-324 in runtime.ts).
</verification>

<success_criteria>
**Maps to ROADMAP SC1 (REQ-PLAN-02):**
- [x] Faux-backed integration test proves `planFeature` emits a task DAG via typed pi-sdk tool calls (addTask, addDependency, editTask[weight], submit) and the proposal apply path produces the expected graph state.
- [x] Cycle-creating addDependency rejected via `skipped[]` (not silently accepted).
- [x] Duplicate addTask convention pinned by a test (one outcome, not flaky).
- [x] submit-before-addTask semantics pinned by a test.
- [x] editTask reweight path exercised end-to-end.
- [x] Planner prompt input contract documented in `src/agents/prompts/plan.ts`.

**Scope Fence (NOT this plan):**
- Merge-train semantics — Phase 6.
- Top-level planner (cross-feature DAG) — Phase 7.
- Repair-loop re-entry into planner (verify → replan cap escalation) — 05-04.
- Verify-agent prompt surface — 05-03.
- FSM end-to-end walk (planning → executing → … → awaiting_merge) — 05-02.
- Modifying `applyGraphProposal` behavior — this plan only pins existing behavior with tests; behavioral changes are out of scope.
</success_criteria>

<output>
After completion, create `.planning/phases/05-feature-lifecycle/05-01-SUMMARY.md` using the standard SUMMARY template. Include:
- The three new test file targets + pass/fail states.
- The documented planner input contract summary (fields threaded).
- Any surprises found about `applyGraphProposal.skipped[]` or duplicate-addTask convention — these inform Phase 7 top-level planner.
</output>
