---
phase: 05-feature-lifecycle
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/agents/tools/feature-phase-host.ts
  - src/agents/tools/schemas.ts
  - src/agents/runtime.ts
  - src/agents/prompts/verify.ts
  - test/unit/agents/verify-contract.test.ts
  - test/integration/feature-phase-agent-flow.test.ts
autonomous: true
requirements:
  - REQ-MERGE-04
must_haves:
  truths:
    - "`getChangedFiles` is git-backed: runs `simpleGit(worktreeDir).raw(['diff', '--name-only', `${baseRef}...HEAD`])` and returns a string[] (empty on no diff)"
    - "`getChangedFiles` accepts an optional `baseRef` arg (default 'main') so Phase 6 merge-train can reuse it with a different base"
    - "Verify prompt renders a dedicated labeled block `Changed Files` positioned between `Plan Summary` and `Execution Evidence`"
    - "Empty-diff path surfaces `No changes on feature branch vs base.` in the Changed Files block; verify agent is instructed via the static prompt to submit `repair_needed` in that case"
    - "`submitVerify` contract is unit-tested: pass outcome recorded, repair_needed outcome recorded with issues[], missing submit throws from runVerifyPhase"
    - "`VerificationSummary` round-trips through `AgentRun.payloadJson` — read-side assertion confirms the submitVerify toolCall and summary are persisted"
  artifacts:
    - path: "src/agents/tools/feature-phase-host.ts"
      provides: "Git-backed getChangedFiles implementation replacing task-result-union"
      contains: "simpleGit"
    - path: "src/agents/tools/schemas.ts"
      provides: "getChangedFiles schema extended with optional baseRef"
      contains: "baseRef: Type.Optional"
    - path: "src/agents/prompts/verify.ts"
      provides: "Verify prompt with dedicated `Changed Files` labeled block + empty-diff instruction"
      contains: "Changed Files"
    - path: "src/agents/runtime.ts"
      provides: "renderPrompt threads changedFiles through to verify template (now async)"
      contains: "changedFiles"
    - path: "test/unit/agents/verify-contract.test.ts"
      provides: "Unit tests for submitVerify contract (pass / repair_needed / missing-submit)"
      contains: "submitVerify"
    - path: "test/integration/feature-phase-agent-flow.test.ts"
      provides: "Extended verify-phase case covering empty-diff path + VerificationSummary persistence"
      contains: "empty diff"
  key_links:
    - from: "src/agents/tools/feature-phase-host.ts::getChangedFiles"
      to: "simple-git diff --name-only ${base}...HEAD"
      via: "simpleGit(worktreeDir).raw(...)"
      pattern: "simpleGit.*raw"
    - from: "src/agents/runtime.ts::renderPrompt"
      to: "src/agents/prompts/verify.ts::VERIFY_PROMPT (Changed Files block)"
      via: "template.render({ changedFiles })"
      pattern: "changedFiles"
    - from: "src/agents/runtime.ts::runVerifyPhase"
      to: "persistPhaseOutputToFeature (VerificationSummary → AgentRun.payloadJson)"
      via: "mutateFeature → scheduler event queue"
      pattern: "persistPhaseOutputToFeature|payloadJson"
---

<objective>
Harden the verify agent's I/O surface so it performs a real pi-sdk agent review against the feature branch diff (REQ-MERGE-04 initial). Three concrete changes: (1) make `getChangedFiles` git-backed (replacing the current task-result-union), (2) thread a dedicated `Changed Files` labeled block into the verify prompt with an empty-diff instruction, (3) pin the `submitVerify` contract (pass / repair_needed / missing-submit) and verify persistence round-trip with unit + integration tests.

Purpose: Satisfies SC3 ("Verify phase runs a real pi-sdk agent review against the feature branch diff, returning pass or issues"). Per RESEARCH O1, the git-backed approach matches CONTEXT § C wording; per RESEARCH O5, the dedicated labeled block (not inline) gives the verify agent a clean anchor. Per CONTEXT § G, the empty-diff path does NOT short-circuit — the agent still runs and records a verdict.

Output:
- Git-backed `getChangedFiles` with optional `baseRef` arg.
- Verify prompt with `Changed Files` labeled block + empty-diff instruction.
- Unit tests `test/unit/agents/verify-contract.test.ts` for the three submitVerify cases.
- Extended integration test covering empty-diff + VerificationSummary persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-feature-lifecycle/05-CONTEXT.md
@.planning/phases/05-feature-lifecycle/05-RESEARCH.md
@.planning/phases/05-feature-lifecycle/05-PATTERNS.md
@.planning/phases/05-feature-lifecycle/05-02-SUMMARY.md

@src/agents/tools/feature-phase-host.ts
@src/agents/tools/schemas.ts
@src/agents/runtime.ts
@src/agents/prompts/verify.ts
@src/orchestrator/conflicts/git.ts
@src/agents/worker/tools/git-diff.ts
@test/integration/feature-phase-agent-flow.test.ts
@test/helpers/feature-lifecycle-fixture.ts

<interfaces>
<!-- Extracted from codebase. Use these directly. -->

From src/agents/tools/feature-phase-host.ts (current getChangedFiles — lines 97-111):
- Currently unions `task.result.filesChanged` across feature's tasks.
- Returns `string[]`.
- Host has access to `this.graph` (FeatureGraph) — can look up `feature.featureBranch`, `feature.worktreeDir` (confirm field names during impl).

From src/orchestrator/conflicts/git.ts (pattern to copy):
- `import { simpleGit } from 'simple-git'`
- `const git = simpleGit(gitDir); const diff = await git.raw(['diff', '--name-only', '--diff-filter=U'])`
- Parse: `diff.split('\n').map(s => s.trim()).filter(Boolean)`.

From src/agents/tools/schemas.ts (getChangedFiles schema — current shape):
- `getChangedFiles: Type.Object({ featureId: Type.Optional(Type.String()) })`
- Extend: add `baseRef: Type.Optional(Type.String())`. Additive; existing callers unaffected.

From src/agents/runtime.ts (renderPrompt — lines 271-324):
- Currently synchronous. Must become `async` for verify phase to await changed-files fetch.
- Callers: `runTextPhase`, `runProposalPhase`, `runVerifyPhase` — all already `async` (line 225 for runVerifyPhase). Safe conversion.
- `template.render({...})` takes labeled inputs; add `changedFiles: string | undefined`.

From src/agents/runtime.ts::runVerifyPhase (line 225+):
- Already throws when `!wasVerifySubmitted` at end of agent loop. That enforces submitVerify-required semantics.
- `persistPhaseOutputToFeature` (lines 425-460) already serializes the `VerificationSummary` into `AgentRun.payloadJson` via `mutateFeature`.

From src/agents/prompts/verify.ts (current lines 52-77):
- Block list: `Success Criteria`, `Plan Summary`, `Execution Evidence`, `Verification Results`, `Prior Decisions`.
- Insert `Changed Files` between `Plan Summary` and `Execution Evidence` (RESEARCH O5 recommendation).
- Static prompt (lines 12-50) has an `Issue raising` section; add a bullet for the empty-diff case.

From test/helpers/feature-lifecycle-fixture.ts (from 05-02):
- Exposes tmp-git worktree, faux verifier, scheduler loop, AgentRun read access via `store`.
- Use this for the extended integration case (empty diff + persistence round-trip).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Git-backed getChangedFiles + schema extension + renderPrompt threading</name>
  <files>src/agents/tools/feature-phase-host.ts, src/agents/tools/schemas.ts, src/agents/runtime.ts, src/agents/prompts/verify.ts</files>
  <behavior>
    - `getChangedFiles({ featureId?, baseRef? })` returns `string[]`:
      - Resolves feature (default: the host's current feature context).
      - `baseRef` defaults to `'main'`.
      - Resolves the feature's worktree directory from the feature record (read `feature.worktreeDir` or equivalent — confirm exact field name during impl from `src/core/types/features.ts`).
      - Runs `simpleGit(worktreeDir).raw(['diff', '--name-only', `${baseRef}...HEAD`])`.
      - Returns parsed lines (trimmed, filter empty).
      - Empty diff → returns `[]` (NOT a sentinel string — the prompt-render layer handles empty-array → "No changes" text).
    - Schema: `getChangedFiles: Type.Object({ featureId: Type.Optional(Type.String()), baseRef: Type.Optional(Type.String()) })` — additive.
    - `renderPrompt` becomes `async`. For `phase === 'verify'`, it invokes the host's `getChangedFiles` (or a thin wrapper over `simpleGit` directly — reuse the host path to avoid two code paths) and transforms the result:
      - `[]` → string `"No changes on feature branch vs base."`
      - non-empty → `changedFiles.map(f => `- ${f}`).join('\n')`
    - For non-verify phases, `changedFiles` is `undefined` and the template omits the block (template must handle undefined → block-skipped).
    - Verify template (`src/agents/prompts/verify.ts`) adds a `Changed Files` labeled block between `Plan Summary` and `Execution Evidence`.
    - Static `VERIFY_PROMPT` text (lines 12-50) gains one bullet under `Issue raising`: `"- if 'Changed Files' shows 'No changes on feature branch vs base.', submit verdict \`repair_needed\` with a blocking raiseIssue naming the missing implementation"`.
  </behavior>
  <action>
    1. Read `src/core/types/features.ts` to confirm the feature-record field holding the worktree path. If it is not stored on the feature record, inject `projectRoot` into the host constructor and derive the worktree path from `projectRoot + feature.featureBranch` (the convention documented in CLAUDE.md: `feat-<name>-<feature-id>`).
    2. In `feature-phase-host.ts`, replace the body of `getChangedFiles` (lines 97-111) with the git-backed impl. Delete the task-result-union code (RESEARCH O1 resolution: replace, don't coexist). Keep the `string[]` return type.
    3. In `schemas.ts`, add `baseRef: Type.Optional(Type.String())` to the `getChangedFiles` schema.
    4. In `runtime.ts::renderPrompt`, convert to `async`. Add the verify-phase branch computing `changedFilesText`. Thread through `template.render({..., changedFiles: changedFilesText})`.
    5. Update all `renderPrompt(...)` callers (`runTextPhase`, `runProposalPhase`, `runVerifyPhase`) to `await` it. They are already async.
    6. In `verify.ts`, add `renderLabeledBlock('Changed Files', getString(input, 'changedFiles'))` between `Plan Summary` and `Execution Evidence`. Use `getStringOrEmpty` / whatever helper omits the block when undefined.
    7. In `verify.ts` static prompt text (lines 12-50), add the empty-diff bullet under `Issue raising`.
    8. Run `npm run typecheck` — expect async-propagation errors at the call sites; fix them.

    CRITICAL: Phase 6 merge-train will re-use `getChangedFiles` with a different `baseRef`. Do NOT hard-code `'main'` inside the helper body beyond the parameter default.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run test/integration/feature-phase-agent-flow.test.ts -t verify</automated>
  </verify>
  <done>getChangedFiles is git-backed and accepts optional baseRef. Verify prompt renders Changed Files block. Existing verify-phase integration tests still pass (with updated assertions for the new block). Typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: submitVerify contract unit tests</name>
  <files>test/unit/agents/verify-contract.test.ts</files>
  <behavior>
    - Test 1 (pass): Script a faux verifier that calls `submitVerify({ outcome: 'pass' })`. After `runVerifyPhase` resolves, assert: the recorded `VerificationSummary` has `ok === true`, `issues: []` (or unset), no throw.
    - Test 2 (repair_needed): Script a faux verifier that calls `raiseIssue({ severity: 'blocking', description: 'auth gap', location: 'src/auth.ts' })` followed by `submitVerify({ outcome: 'repair_needed' })`. Assert: `VerificationSummary.ok === false`, `issues.length === 1`, issue has correct severity + description + location.
    - Test 3 (missing submit): Script a faux verifier that calls only `fauxText('I have nothing to say')` with `stopReason: 'endTurn'` — no submitVerify. Assert: `runVerifyPhase` rejects (throws) with a message matching `/submitVerify/i`.
    - Test 4 (auto-downgrade): Script a verifier that calls `raiseIssue({severity:'blocking'})` then `submitVerify({outcome:'pass'})` — per `feature-phase-host.ts:152-220`, host auto-downgrades to `repair_needed` when blocking/concern present. Assert `ok===false`.
  </behavior>
  <action>
    Place file at `test/unit/agents/verify-contract.test.ts`. Reuse `createFeatureLifecycleFixture` from 05-02 if it supports a verify-only mode; if too heavyweight for unit-level, use a minimal `AgentRuntime + InMemoryStore + faux verifier` harness (copy from `feature-phase-agent-flow.test.ts:247-294` with the pool wiring stripped). Do NOT invoke the scheduler loop — call `runtime.verifyFeature(featureId)` directly and inspect the returned `VerificationSummary` (or read from `AgentRun.payloadJson` via store after completion).
  </action>
  <verify>
    <automated>npx vitest run test/unit/agents/verify-contract.test.ts</automated>
  </verify>
  <done>Four unit tests pass. submitVerify contract is pinned: pass/repair_needed/missing-submit/auto-downgrade outcomes each have a test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Empty-diff integration case + VerificationSummary persistence round-trip</name>
  <files>test/integration/feature-phase-agent-flow.test.ts</files>
  <behavior>
    - New case `"verify phase on empty diff instructs repair_needed"`:
      - Fixture seeds a feature whose feature branch has zero commits beyond base (so `git diff --name-only main...HEAD` returns empty).
      - Faux verifier reads the rendered prompt (via fixture's prompt-capture hook if available; otherwise the transcript-side need only respond correctly) and emits `raiseIssue(blocking, 'no implementation')` + `submitVerify({outcome:'repair_needed'})`.
      - Assert: `VerificationSummary` with `ok===false`, 1 issue, `AgentRun.payloadJson` contains the summary.
      - Assert the prompt rendered contains `"No changes on feature branch vs base."` (either via a prompt-capture fixture hook or by inspecting a stored prompt trace — use whichever the existing test file already does; see line 299 area for precedent).
    - New case `"VerificationSummary persists to AgentRun.payloadJson"`:
      - Faux verifier does a simple pass run.
      - After scheduler drains, read the AgentRun row for phase='verify' from store. Assert `payloadJson` parses to a `VerificationSummary` with the expected shape: `{ ok: true, issues: [], summary?: string }`. Also assert at least one recorded toolCall is `submitVerify` (per CONTEXT § M acceptance scope).
  </behavior>
  <action>
    Extend `test/integration/feature-phase-agent-flow.test.ts`. Use the shared fixture from 05-02 where feasible. The empty-diff case requires the fixture to NOT pre-create any commits on the feature branch beyond the seed commit; check whether `createFeatureLifecycleFixture` supports a `skipFeatureBranchCommits: true` option and add one if not (amend the fixture helper in 05-02's file, extending — not replacing — it).

    For the persistence round-trip assertion, read `store.getAgentRun(agentRunId)` (or equivalent read port — confirm exact method name from `src/orchestrator/ports/index.ts`) and parse `payloadJson`.
  </action>
  <verify>
    <automated>npx vitest run test/integration/feature-phase-agent-flow.test.ts -t "empty diff"
npx vitest run test/integration/feature-phase-agent-flow.test.ts -t "VerificationSummary persists"</automated>
  </verify>
  <done>Both new integration cases pass. Empty-diff path is pinned; VerificationSummary persistence round-trips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify agent (LLM) → orchestrator | Verify agent emits verdict + issues; orchestrator trusts `submitVerify` output to drive repair-loop routing. |
| feature worktree → verify prompt | Changed-files list is read from git state and embedded in the prompt; malformed paths could mislead the verifier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | Verify agent skips submitVerify and claims completion | mitigate | `runVerifyPhase` already throws when `!wasVerifySubmitted`; Task 2 pins this with a unit test. |
| T-05-03-02 | Spoofing | Agent calls `submitVerify({outcome:'pass'})` while raising blocking issues | mitigate | Host auto-downgrades to `repair_needed` when issues present (`feature-phase-host.ts:152-220`); Task 2 Test 4 pins this. |
| T-05-03-03 | Information Disclosure | Changed-files list leaks paths outside the feature worktree | mitigate | `simpleGit(worktreeDir)` is scoped to the feature's worktree; git diff against `${baseRef}...HEAD` cannot surface paths outside the repo. |
| T-05-03-04 | Tampering | Empty-diff feature silently passes verify | mitigate | CONTEXT § G — no orchestrator short-circuit. Prompt instructs agent to emit `repair_needed` on empty diff. Task 3 Test 1 pins this. |
| T-05-03-05 | Input Validation | `baseRef` parameter allows arbitrary git ref injection | accept | Local-only tool, single-user orchestrator per ASVS V4 scope. Trust boundary does not include untrusted `baseRef` input (caller is always Phase 5/6 orchestrator, not user input). |
</threat_model>

<verification>
- `npm run typecheck` — renderPrompt async propagation is clean.
- `npx vitest run test/unit/agents/verify-contract.test.ts` — four submitVerify contract tests pass.
- `npx vitest run test/integration/feature-phase-agent-flow.test.ts -t verify` — existing + new verify cases all pass.
- `npx vitest run test/integration/feature-phase-agent-flow.test.ts -t "empty diff"` — empty-diff case passes.
- `npx vitest run test/integration/feature-phase-agent-flow.test.ts -t "VerificationSummary persists"` — persistence round-trip passes.
- `npm run check` — no regressions.
- Manual: rendered verify prompt (via any captured prompt output or snapshot fixture) contains the `Changed Files` block positioned between `Plan Summary` and `Execution Evidence`.
</verification>

<success_criteria>
**Maps to ROADMAP SC3 (REQ-MERGE-04 initial):**
- [x] `getChangedFiles` is git-backed — uses `git diff --name-only ${baseRef}...HEAD`.
- [x] Optional `baseRef` parameter supports Phase 6 merge-train reuse (not hard-coded to `main`).
- [x] Verify prompt has a dedicated `Changed Files` labeled block (RESEARCH O5 resolution: dedicated, not inline).
- [x] Empty-diff path renders `No changes on feature branch vs base.` and prompt instructs `repair_needed`.
- [x] submitVerify contract pinned: pass / repair_needed / missing-submit / auto-downgrade each have a unit test.
- [x] VerificationSummary persists to `AgentRun.payloadJson` with round-trip assertion.

**Scope Fence (NOT this plan):**
- Repair-loop fan-out (`enqueueVerifyRepairs`) — 05-04.
- Hallucinated-progress gate — 05-04.
- Merge-train verification with non-main base ref — Phase 6 (consumer of `baseRef` param).
- Verify model selection (`roles.verifier`) — already wired in Phase 2; unchanged here.
- ci_check shell path — unchanged (CONTEXT § K).
- Top-level planner context for verify issues — Phase 7.
</success_criteria>

<output>
After completion, create `.planning/phases/05-feature-lifecycle/05-03-SUMMARY.md`. Include:
- Exact field name resolved for feature worktree path (projectRoot + featureBranch derivation vs stored field).
- Any non-obvious schema/prompt ordering decisions.
- Confirmation that `renderPrompt` is async and all three callers (runTextPhase / runProposalPhase / runVerifyPhase) `await` it.
- Phase 6 handoff note: `getChangedFiles` accepts `baseRef` and the verify prompt surface is ref-agnostic.
</output>
