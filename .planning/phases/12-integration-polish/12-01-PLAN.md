---
phase: 12-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/prompt-to-main-e2e.test.ts
  - test/integration/verify-flake-audit.test.ts
  - test/helpers/feature-lifecycle-fixture.ts
autonomous: true
requirements:
  - REQ-PLAN-01
  - REQ-PLAN-02
  - REQ-EXEC-01
  - REQ-EXEC-02
  - REQ-INBOX-01
  - REQ-MERGE-01
  - REQ-MERGE-02
  - REQ-MERGE-04
must_haves:
  truths:
    - "A deterministic Vitest integration test proves the non-TUI prompt-to-main-style lifecycle: planner proposal, approval, worker execution, one answered inbox item, verify pass, merge-train drain, and main containing the expected work."
    - "The integration proof uses the existing faux-provider and in-process worker harness; it makes no live LLM calls and does not use the TUI e2e lane."
    - "A deterministic verify-agent audit runs the known-good verify path five isolated times and fails unless all five runs pass."
    - "12-01 evidence is grep-friendly for 12-03 traceability without claiming README/source-install, TUI e2e, or final v1 green-out coverage."
  artifacts:
    - path: "test/integration/prompt-to-main-e2e.test.ts"
      provides: "Scripted non-TUI prompt-to-main lifecycle proof"
      contains: "prompt-to-main"
    - path: "test/integration/verify-flake-audit.test.ts"
      provides: "Five-repeat deterministic verify-agent flake-rate audit"
      contains: "5/5"
    - path: "test/helpers/feature-lifecycle-fixture.ts"
      provides: "Narrow fixture support for merge-train drain/root-main assertions if existing fixture layout requires it"
      contains: "featureWorktreePath"
  key_links:
    - from: "test/integration/prompt-to-main-e2e.test.ts"
      to: "test/helpers/feature-lifecycle-fixture.ts"
      via: "createFeatureLifecycleFixture"
      pattern: "createFeatureLifecycleFixture"
    - from: "test/integration/prompt-to-main-e2e.test.ts"
      to: "inbox help response"
      via: "respondToInboxHelp or LocalWorkerPool respondToHelp path"
      pattern: "respondToInboxHelp|respondToHelp"
    - from: "test/integration/prompt-to-main-e2e.test.ts"
      to: "merge train integration runner"
      via: "scheduler.step/stepUntil until collabControl is merged"
      pattern: "collabControl.*merged"
    - from: "test/integration/verify-flake-audit.test.ts"
      to: "PiFeatureAgentRuntime.verifyFeature"
      via: "faux submitVerify pass transcript per isolated attempt"
      pattern: "submitVerify"
---

<objective>
Create the Phase 12-01 executable proof for the non-TUI v1 integration path.

Purpose: Phase 12 must prove the core loop end-to-end before final polish. This plan covers only the scripted Vitest path and deterministic verify-agent flake audit from the Phase 12 split: no TUI e2e smoke, no README/source-install dry-run, and no final v1 traceability green-out.

Output: Two deterministic Vitest integration tests and, only if needed for real merge-train drain assertions, a narrow test fixture extension.
</objective>

<execution_context>
@/home/alpine/.claude/get-shit-done/workflows/execute-plan.md
@/home/alpine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/alpine/vcode0/CLAUDE.md
@/home/alpine/vcode0/.planning/PROJECT.md
@/home/alpine/vcode0/.planning/ROADMAP.md
@/home/alpine/vcode0/.planning/STATE.md
@/home/alpine/vcode0/.planning/REQUIREMENTS.md
@/home/alpine/vcode0/.planning/phases/12-integration-polish/12-CONTEXT.md
@/home/alpine/vcode0/.planning/phases/12-integration-polish/12-01-RESEARCH.md
@/home/alpine/vcode0/.planning/phases/12-integration-polish/12-01-PATTERNS.md
@/home/alpine/vcode0/.planning/phases/12-integration-polish/12-01-VALIDATION.md
@/home/alpine/vcode0/.planning/phases/11-documentation-diagnostic-tooling/11-03-SUMMARY.md
@/home/alpine/vcode0/docs/operations/testing.md
@/home/alpine/vcode0/docs/foundations/newcomer.md
@/home/alpine/vcode0/test/helpers/feature-lifecycle-fixture.ts
@/home/alpine/vcode0/test/integration/feature-lifecycle-e2e.test.ts
@/home/alpine/vcode0/test/integration/worker-smoke.test.ts
@/home/alpine/vcode0/test/integration/feature-phase-agent-flow.test.ts
@/home/alpine/vcode0/test/integration/merge-train.test.ts
@/home/alpine/vcode0/src/orchestrator/scheduler/integration-runner.ts
@/home/alpine/vcode0/src/agents/runtime.ts

<interfaces>
<!-- Key contracts and patterns executor must use directly. Avoid exploratory rewrites. -->

From test/helpers/feature-lifecycle-fixture.ts:
```typescript
export function createFeatureLifecycleFixture(
  options: CreateFeatureLifecycleFixtureOptions = {},
): FeatureLifecycleFixture;

export interface FeatureLifecycleFixture {
  faux: FauxProviderRegistration;
  graph: InMemoryFeatureGraph;
  store: InMemoryStore;
  sessionStore: InMemorySessionStore;
  config: GvcConfig;
  scheduler: SchedulerLoop;
  pool: LocalWorkerPool;
  harness: InProcessHarness;
  workerMessages: WorkerToOrchestratorMessage[];
  tmpDir: string;
  seedFeature(featureId: string, options?: SeedFeatureOptions): Feature;
  featureWorktreePath(featureBranch: string): string;
  stepUntil(predicate: () => boolean, options?: { maxTicks?: number; now?: number }): Promise<number>;
  teardown(): Promise<void>;
}
```

From test/integration/harness/faux-stream.ts:
```typescript
export function createFauxProvider(options?: RegisterFauxProviderOptions): FauxProviderRegistration;
export { fauxAssistantMessage, fauxText, fauxThinking, fauxToolCall };
```

From src/agents/tools/feature-phase-host.ts verify semantics:
```typescript
submitVerify(args: SubmitVerifyOptions): VerificationSummary;
// outcome is pass only when args.outcome === 'pass' and no blocking/concern issue was raised.
```

From src/orchestrator/scheduler/integration-runner.ts:
```typescript
// runIntegrationIfPending performs: rebase onto main -> shell verify -> agent verify with run-integration:<featureId> -> ff-only merge -> feature_integration_complete.
```
</interfaces>

<scope_boundaries>
- Implement CONTEXT 12-01 scope exactly: non-TUI scripted integration proof plus deterministic verify-agent audit.
- Do not create or modify `test/integration/tui/**`, README/source-install docs, final traceability tables, or live-provider audit code.
- Use existing Vitest integration harness and faux-provider patterns. Do not hand-roll a fake planner, fake worker, fake verifier, or custom mini-orchestrator.
- Keep worker concurrency at fixture default `maxConcurrency: 1` unless a file-local reason proves otherwise, because the shared faux response queue is deterministic only when consumed in scripted order.
</scope_boundaries>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build prompt-to-main Vitest lifecycle proof</name>
  <files>test/integration/prompt-to-main-e2e.test.ts, test/helpers/feature-lifecycle-fixture.ts</files>
  <read_first>
    - /home/alpine/vcode0/.planning/phases/12-integration-polish/12-CONTEXT.md
    - /home/alpine/vcode0/.planning/phases/12-integration-polish/12-01-RESEARCH.md
    - /home/alpine/vcode0/.planning/phases/12-integration-polish/12-01-PATTERNS.md
    - /home/alpine/vcode0/test/helpers/feature-lifecycle-fixture.ts
    - /home/alpine/vcode0/test/integration/feature-lifecycle-e2e.test.ts
    - /home/alpine/vcode0/test/integration/worker-smoke.test.ts
    - /home/alpine/vcode0/test/integration/merge-train.test.ts
    - /home/alpine/vcode0/src/orchestrator/scheduler/integration-runner.ts
  </read_first>
  <behavior>
    - Test name contains `prompt-to-main` and `merge-train drains` so 12-03 can grep it.
    - Given one seeded feature that represents a user prompt, the test drives planner proposal creation and approval using scheduler events and faux planner responses.
    - Given one worker task that calls `request_help`, the test observes an unresolved `agent_help` inbox item, answers it through the existing runtime/orchestrator help response path, and then the worker completes.
    - Given worker completion, the test asserts at least one `commit_done` worker frame has `trailerOk === true` and a SHA-like value, proving REQ-EXEC-02 evidence is not faked.
    - Given feature verify and merge-train integration both receive deterministic `submitVerify({ outcome: 'pass' })` faux responses, the test drives scheduler ticks until the feature reaches `collabControl === 'merged'` and root/main contains the expected committed file or commit evidence.
  </behavior>
  <action>
    Create `test/integration/prompt-to-main-e2e.test.ts` by composing the patterns in `feature-lifecycle-e2e.test.ts`, `worker-smoke.test.ts`, and `merge-train.test.ts`. Use `createFeatureLifecycleFixture()` as the base seam, seed a single feature, pre-create the task file(s) in the feature worktree, script faux responses in deterministic order, approve the planner proposal, answer exactly one inbox help item, and drive the scheduler until merge-train completion. If the existing fixture root repository cannot see the feature branch for `runIntegrationIfPending()` fast-forward merge, extend only `test/helpers/feature-lifecycle-fixture.ts` to make the feature branch/ref visible to the root repo or to expose a helper that syncs the feature worktree commit into the expected integration layout. Do not bypass `SchedulerLoop`, do not directly set graph state to merged, and do not introduce TUI or live-provider entrypoints. This implements CONTEXT decisions for deterministic executable tests, existing faux-provider harnesses, one inbox answer, merge-train drain, and 12-01-only non-TUI scope.
  </action>
  <verify>
    <automated>npm run test:integration -- prompt-to-main-e2e</automated>
  </verify>
  <acceptance_criteria>
    - `test/integration/prompt-to-main-e2e.test.ts` exists and contains `prompt-to-main` in at least one `describe` or `it` name.
    - The test imports `createFeatureLifecycleFixture` and uses `fauxAssistantMessage`, `fauxToolCall`, and `fauxText` rather than a custom model fake.
    - The test contains an assertion over an unresolved `agent_help` inbox item or existing help-response delivery result, and then asserts the same scenario continues after the answer.
    - The test asserts `commit_done` frames with `trailerOk` and SHA shape.
    - The test asserts `collabControl` becomes `merged` and checks main/root git history or files for the expected task output after merge-train drain.
    - The test does not import from `test/integration/tui/` and does not reference `@microsoft/tui-test`.
  </acceptance_criteria>
  <done>Focused prompt-to-main integration command passes and proves planner/approval/inbox/worker/verify/merge-train/main evidence without TUI or live LLM calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build deterministic verify-agent flake-rate audit</name>
  <files>test/integration/verify-flake-audit.test.ts</files>
  <read_first>
    - /home/alpine/vcode0/.planning/phases/12-integration-polish/12-CONTEXT.md
    - /home/alpine/vcode0/.planning/phases/12-integration-polish/12-01-RESEARCH.md
    - /home/alpine/vcode0/.planning/phases/12-integration-polish/12-01-PATTERNS.md
    - /home/alpine/vcode0/test/integration/feature-phase-agent-flow.test.ts
    - /home/alpine/vcode0/src/agents/runtime.ts
    - /home/alpine/vcode0/src/agents/prompts/verify.ts
    - /home/alpine/vcode0/src/agents/tools/feature-phase-host.ts
  </read_first>
  <behavior>
    - Test name contains `verify-agent flake audit` and `5/5` so 12-03 can cite it.
    - The audit runs five known-good verify attempts in one deterministic test using isolated per-attempt project roots/fixtures or equivalent per-attempt state reset.
    - Each attempt scripts a real faux-backed `submitVerify({ outcome: 'pass' })` verify-agent turn and records whether persisted verify payload is `{ ok: true, outcome: 'pass' }`.
    - The audit fails unless `passCount === 5` and emits assertion context containing attempts, pass count, and failures.
  </behavior>
  <action>
    Create `test/integration/verify-flake-audit.test.ts` following the verify-agent fixture pattern from `feature-phase-agent-flow.test.ts`. Use real `PiFeatureAgentRuntime.verifyFeature()`/scheduler verify flow with `createFauxProvider()` and the existing in-memory store/session store patterns. For each of five attempts, isolate temporary git/project state, create a known-good feature diff, append any required prior ci_check evidence, script the verify faux response with `submitVerify({ outcome: 'pass', summary: 'known-good verify pass ...' })` plus closing text, run the verify path, parse the persisted verify run payload, and collect pass/fail evidence. Assert `passCount` is exactly `5` out of `5`; because five repeats cannot represent 90% exactly, implement the CONTEXT decision to require 5/5 consistency. Do not use Vitest retries/repeats to hide failed attempts, and do not call live providers.
  </action>
  <verify>
    <automated>npm run test:integration -- verify-flake-audit</automated>
  </verify>
  <acceptance_criteria>
    - `test/integration/verify-flake-audit.test.ts` exists and contains both `verify-agent flake audit` and `5/5` in test text.
    - The test loops over exactly five attempts or defines `const attempts = 5` and asserts `passCount` equals `attempts` with `attempts === 5`.
    - The test uses `fauxToolCall('submitVerify', { outcome: 'pass', ... })` for each known-good verify attempt.
    - The test collects failures into an array/string included in the final assertion context.
    - The test unregisters the faux provider and removes tmp project roots/worktrees after attempts.
    - No code path reads provider API keys or uses a live model provider.
  </acceptance_criteria>
  <done>Focused verify audit command passes and proves deterministic 5/5 known-good verify-agent consistency.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused and full verification for 12-01 evidence</name>
  <files>test/integration/prompt-to-main-e2e.test.ts, test/integration/verify-flake-audit.test.ts, test/helpers/feature-lifecycle-fixture.ts</files>
  <read_first>
    - /home/alpine/vcode0/CLAUDE.md
    - /home/alpine/vcode0/package.json
    - /home/alpine/vcode0/.planning/phases/12-integration-polish/12-CONTEXT.md
  </read_first>
  <action>
    Run the focused 12-01 integration checks, then project verification using existing package scripts. If formatting/lint changes are needed, run `npm run check:fix` and keep changes limited to the 12-01 test files and the narrow fixture extension from Task 1. Do not add a package script in 12-01; use the existing `npm run test:integration -- ...` and `npm run check` commands for all verification evidence.
  </action>
  <verify>
    <automated>npm run test:integration -- prompt-to-main-e2e verify-flake-audit && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:integration -- prompt-to-main-e2e verify-flake-audit` exits 0.
    - `npm run check` exits 0 after any required `npm run check:fix` formatting pass.
    - `rg "prompt-to-main|verify-agent flake audit|5/5" test/integration` finds grep-friendly 12-01 evidence.
    - `rg "@microsoft/tui-test|test/integration/tui|source-install|fresh-clone|traceability green" test/integration/prompt-to-main-e2e.test.ts test/integration/verify-flake-audit.test.ts` finds no 12-02/12-03 scope leakage.
  </acceptance_criteria>
  <done>Focused 12-01 tests and full project check are green; evidence names are traceability-friendly and scope boundaries remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Faux provider -> agent tool host | Scripted assistant tool calls cross into typed tool schemas and scheduler state changes. |
| Worker commands -> tmp git worktree | Worker `run_command` calls mutate files and git state inside temporary test worktrees. |
| Test harness -> merge train root repo | Integration runner rebases/verifies/fast-forwards root `main` in tmp repositories. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | I | `test/integration/*` faux-backed agent tests | mitigate | Register only `createFauxProvider()` on the configured test model and do not read live provider credentials or call live models. |
| T-12-01-02 | T | Worker `run_command` transcript in `prompt-to-main-e2e.test.ts` | mitigate | Script only expected `git add`, `git commit`, and file-write-safe commands inside fixture worktrees; rely on existing worker cwd/write enforcement and teardown. |
| T-12-01-03 | D | Five-repeat verify audit runtime | mitigate | Keep attempts isolated and deterministic, avoid parallel worker concurrency, and run focused commands before full `npm run check`. |
| T-12-01-04 | R | Phase 12 traceability evidence | mitigate | Use grep-friendly test names and assertion text for `prompt-to-main`, `verify-agent flake audit`, and `5/5` so later traceability can cite executable proof. |
</threat_model>

<verification>
Run, in order:

```bash
npm run test:integration -- prompt-to-main-e2e
npm run test:integration -- verify-flake-audit
npm run test:integration -- prompt-to-main-e2e verify-flake-audit
npm run check
```

If formatting/lint fails, run `npm run check:fix`, then rerun the focused command and `npm run check`.
</verification>

<success_criteria>
- Phase 12 success criterion 1 is covered for the non-TUI path by a deterministic Vitest test: prompt/planner proposal, approval, task execution, one answered inbox item, verify pass, merge-train drain, and main containing expected work.
- Phase 12 success criterion 2 is covered by a deterministic known-good verify-agent audit with 5/5 passing attempts.
- The plan does not claim or implement Phase 12 success criteria 3, 4, or 5; those remain for 12-02 and 12-03.
- All selected requirements in frontmatter have executable evidence in the new tests or direct harness assertions: planner proposal (REQ-PLAN-01/02), worker worktree and squash/trailer evidence (REQ-EXEC-01/02), inbox help route (REQ-INBOX-01), strict merge-train integration and agent review (REQ-MERGE-01/02/04).
</success_criteria>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 12 SC1 | Scripted end-to-end scenario: prompt -> draft features -> execute -> answer one inbox item -> merge-train drains -> main contains expected commits | 12-01 | COVERED | Task 1 plans the non-TUI Vitest equivalent per CONTEXT boundary. |
| GOAL | Phase 12 SC2 | Verify-agent flake-rate audit: >=90% consistency on known-good branch across 5 repeats | 12-01 | COVERED | Task 2 uses 5/5 because 5 repeats cannot represent 90% exactly per CONTEXT. |
| GOAL | Phase 12 SC3 | TUI e2e smoke tests | NONE | EXCLUDED | Explicitly 12-02 per CONTEXT deferred/scope split. |
| GOAL | Phase 12 SC4 | README/source-install runbook fresh-clone dry-run | NONE | EXCLUDED | Explicitly 12-03 per CONTEXT deferred/scope split. |
| GOAL | Phase 12 SC5 | All v1 REQ traceability green-out | NONE | EXCLUDED | Explicitly 12-03 per CONTEXT deferred/scope split. |
| REQ | REQ-PLAN-01 | Top-level planner turns prompt into feature DAG via inline chat | 12-01 | COVERED | Task 1 uses scheduler/planner proposal seam, not TUI typing. |
| REQ | REQ-PLAN-02 | Feature-level planner turns feature into task DAG | 12-01 | COVERED | Task 1 approves feature planning proposal with task creation. |
| REQ | REQ-EXEC-01 | Each task runs as child-process/pi-sdk Agent in own worktree | 12-01 | COVERED | Task 1 uses LocalWorkerPool/InProcessHarness and fixture worktree seam. |
| REQ | REQ-EXEC-02 | Tasks produce trailer-valid squash/commit evidence | 12-01 | COVERED | Task 1 asserts `commit_done.trailerOk` and SHA. |
| REQ | REQ-INBOX-01 | Agent `await_response`/`request_help` routes to inbox | 12-01 | COVERED | Task 1 observes and answers one `agent_help` item. |
| REQ | REQ-MERGE-01 | Merge train serializes integration; main never bad | 12-01 | COVERED | Task 1 drains merge train after passing verification and asserts merged/main evidence. |
| REQ | REQ-MERGE-02 | Queue head rebases, verifies, merges/ejects | 12-01 | COVERED | Task 1 drives integration runner happy path. |
| REQ | REQ-MERGE-04 | Verification before merge is agent review | 12-01 | COVERED | Task 1 scripts `run-integration:<featureId>` verify pass; Task 2 audits verify agent. |
| RESEARCH | R-01 | Use existing Vitest integration harness and faux provider, no live model calls | 12-01 | COVERED | Tasks 1 and 2 require `createFeatureLifecycleFixture`/`createFauxProvider`. |
| RESEARCH | R-02 | Extend lifecycle fixture narrowly if merge layout blocks root-main assertion | 12-01 | COVERED | Task 1 includes exact allowed fixture extension. |
| RESEARCH | R-03 | Keep worker concurrency deterministic because faux queue is shared | 12-01 | COVERED | Scope boundaries require default maxConcurrency 1. |
| RESEARCH | R-04 | Use explicit five-run loop rather than hidden Vitest retry/repeats | 12-01 | COVERED | Task 2 requires explicit attempts/passCount/failures. |
| CONTEXT | C-01 | 12-01 covers non-TUI integration proof and verify audit only | 12-01 | COVERED | Scope boundaries and tasks exclude 12-02/12-03 work. |
| CONTEXT | C-02 | Prefer deterministic executable tests over manual scripts | 12-01 | COVERED | Tasks create Vitest tests. |
| CONTEXT | C-03 | Use existing Vitest/faux-provider patterns rather than live model calls | 12-01 | COVERED | Tasks require faux patterns and no live provider. |
| CONTEXT | C-04 | Run verify audit 5 repeats and fail below threshold, use 5/5 unless richer denominator exists | 12-01 | COVERED | Task 2 implements 5/5. |
| CONTEXT | C-05 | Keep TUI e2e lane out of 12-01 due known SIGSEGV history | 12-01 | COVERED | Scope boundaries and acceptance grep prevent TUI imports. |
| CONTEXT | Deferred | README/source-install and final v1 traceability | NONE | EXCLUDED | Deferred to 12-03; no plan task implements it. |
</source_audit>

<output>
After completion, create `/home/alpine/vcode0/.planning/phases/12-integration-polish/12-01-SUMMARY.md` with focused test results, any fixture extension made, and handoff notes for 12-02/12-03.
</output>
