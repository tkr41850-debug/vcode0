# core

Pure domain logic for the orchestration model.

This directory owns graph/state types, scheduling rules, warning evaluation, and shared contracts that should not depend on concrete runtime, persistence, git, or UI implementations.
