# git

Git and worktree operations.

This directory owns feature branch creation, task worktree lifecycle, merge-train operations, overlap scanning helpers, rebase/merge helpers, and the git-specific contract surface consumed by the orchestrator.
