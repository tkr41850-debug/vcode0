# orchestrator

Application service layer for driving the system.

This directory coordinates scheduler flow, feature lifecycle transitions, conflict handling, summary transitions, and other orchestration use cases on top of the core domain model and adapter ports.
