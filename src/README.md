# src

Application entry and composition root.

This directory owns process startup, config loading, and wiring the concrete packages into the runnable gvc0 application. It should stay thin and avoid re-implementing package logic.
