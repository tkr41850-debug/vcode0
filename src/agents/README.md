# agents

Agent-driven planning and restructuring logic.

This package owns planner/replanner behavior, prompts, and graph-mutation tools used by agent workflows, without owning the live worker runtime itself.
